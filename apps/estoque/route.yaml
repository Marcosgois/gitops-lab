apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: estoque
  namespace: demos
spec:
  to:
    kind: Service
    name: estoque
  port:
    targetPort: 8080
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
