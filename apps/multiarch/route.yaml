apiVersion: route.openshift.io/v1
kind: Route
metadata: {name: multiarch-demo, namespace: multiarch-demo}
spec:
  to: {kind: Service, name: multiarch-demo}
  port: {targetPort: 8080}
  tls: {termination: edge, insecureEdgeTerminationPolicy: Redirect}
